<!DOCTYPE html>
<html>
  <head>
    <title>CompetitionRails</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <% if flash %>
      <% flash.keys.each do |key| %>
        <p><%= flash[key] %></p>
      <% end %>
    <% end %>

    <%= yield %>
  </body>
</html>
